PORT=5000
MONGO_URI=mongodb://127.0.0.1:27017/samvid_os
JWT_SECRET=change_this_to_a_long_random_secret

# Comma-separated allowed origins.
# Keep empty in local dev to allow localhost and same-origin requests.
CORS_ORIGIN=

# Optional tuning
TRUST_PROXY=false
MONGO_AUTO_INDEX=true
